// max function that takes 2 number as arguments and returns the greatest
const max = (num1, num2) => (num1 > num2 ? num1 : num2);

// min function that takes 2 number as arguments and returns the lowest
const min = (num1, num2) => (num1 < num2 ? num1 : num2);
